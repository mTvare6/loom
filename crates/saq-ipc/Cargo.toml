[package]
name = "saq-ipc"
version = "0.1.0"
edition = "2024"

[dependencies]
tracing = "0.1.44"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
directories = "6.0.0"
mio = {version = "1.2.3", features = ["net", "os-poll"]}
